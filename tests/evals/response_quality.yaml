name: response_quality_assessment

cases:
  - name: weather_helpful_response
    inputs: "What's the weather in Paris?"
    metadata:
      category: response_quality
    evaluators:
      - SkillWasLoaded:
          skill_name: weather
      - ResponseNotEmpty:
          min_length: 100
      - ResponseContains:
          text: paris

  - name: code_review_constructive
    inputs: "Review this function: def calculate(x, y): return x + y"
    metadata:
      category: response_quality
    evaluators:
      - SkillWasLoaded:
          skill_name: code_review
      - ResponseNotEmpty:
          min_length: 200
      - ResponseContains:
          text: function

  - name: weather_with_units
    inputs: "What's the temperature in Berlin?"
    metadata:
      category: response_quality
    evaluators:
      - SkillWasLoaded:
          skill_name: weather
      - ToolWasCalled:
          tool_name: http_get_tool
      - ResponseContains:
          text: berlin

  - name: code_review_security_focus
    inputs: "Check this for SQL injection: user_query = f'SELECT * FROM users WHERE id={id}'"
    metadata:
      category: response_quality
    evaluators:
      - SkillWasLoaded:
          skill_name: code_review
      - ResponseContains:
          text: injection
      - ResponseNotEmpty:
          min_length: 150

evaluators:
  - MaxDuration: 30.0
